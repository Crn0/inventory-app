extends layout
block css
block content
	h1 #{ title }
	div
		if abilities.length
			ul
				each ability in abilities
					li
						+link(ability.url)
							p #[strong Name:] #{ ability.name }
		else 
			p There are no abilities.